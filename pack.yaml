schema: typikon.pack/v0.1

id: oca-experimental
name: Orthodox Church in America (experimental fixture)
version: 0.1.0

calendar:
  fixed: pack_defined
  paschalion: orthodox

definitions:
  services: services/
  observances: observances/
  rules: rules/
  authorities: authorities/
